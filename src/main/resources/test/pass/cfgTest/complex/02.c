#include <bits/stdc++.h>

#define inf 0x3f
#define LLINF 1e18
#define INF 0x3f3f3f3f

using namespace std;

signed main() {
    int i = 0;
    cin >> i;
    if (i > 0) {
        int j;
        cin >> j;
        for (int k = 0; k < j; k ++) {
            cout << k << endl;
        }
    } else {
        cout << i << endl;
    }
    return 0;
}