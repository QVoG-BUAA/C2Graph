#include <bits/stdc++.h>

#define inf 0x3f
#define LLINF 1e18
#define INF 0x3f3f3f3f

using namespace std;

signed main() {
    if (T > 2) {
        cout << g << endl;
        cout << h << endl;
        while (g > 0) {
            cout << gg << endl;
            cout << gg2 << endl;
            while (k > 0) {
                cout << mm << endl;
                cout << mm2 << endl;
            }
        }
    }
    int a;
    cout << e << endl;
}